# run FootFallField's Processing sketch 
# need to be in the right directory so image paths work
echo "Starting FootFallField"
sleep 10s
cd ~/FootFallField/processing
processing-java --sketch=FootFallField --run

